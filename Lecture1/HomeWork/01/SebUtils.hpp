#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

namespace seb {
	namespace str {
		string lowercase(const string&);
		string uppercase(const string&);
	}

	namespace io {
	}

	namespace gen {
	}
}
